export const PARAMS = {
  TODO_ID: "todoId",
};

export const HOME = "/";
export const ADD_TODO = `${HOME}add-todo`;
export const SEE_TODOS = `${HOME}see-todos`;
export const EDIT_TODO = `${HOME}edit-todo`;
export const SEE_USERS = `${HOME}see-users`;
